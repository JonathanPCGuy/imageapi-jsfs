import express from 'express';
import {ImageRequest} from '../imageRequest/imageRequest';

// can i make a class that holds the image parameters in one "class"?

const getImagePath = async(imgRequest: ImageRequest) : Promise<string> => {
    // return types?

    // see if the file is present

    // if not then
    return "hello";
};

//const isFileCached = async()